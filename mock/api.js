import userlist from './userlist.json'

export default {
  // '/api/userlist': userlist,
  // 'POST /api/userdetail': (req, res) => {
  //   const { ename } = req.body;
  //   const hero = userlist.filter(item => item.id === parseInt(id, 10))[0];
  //   res.send(hero);
  // },
}
